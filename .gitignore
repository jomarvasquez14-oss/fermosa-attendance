# Dependencies
node_modules/

# Build outputs
dist/
build/
.expo/
web-build/
*.tsbuildinfo

# Environment / secrets — never commit
.env
.env.*
!.env.example
**/google-service-account*.json

# Supabase local
supabase/.branches/
supabase/.temp/

# Bulk import — filled sheets + generated logins hold real staff data
scripts/bulk-import/branches.csv
scripts/bulk-import/employees.csv
scripts/bulk-import/holidays.csv
scripts/bulk-import/credentials.csv

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Database backups — real staff PII + salaries, never commit
backups/

# Local backup output (real staff PII + salaries)
backup-out/
